[project]
name = "ccvl-harness"
version = "0.1.0"
description = "Deterministic cross-platform verification harness for ccvl"
requires-python = "==3.13.*"
dependencies = [
  "pypdf==6.16.2",
]

[tool.uv]
package = false
